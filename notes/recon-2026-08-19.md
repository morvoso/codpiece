# llm-host recon — 2026-08-19 (session 1)

Access: `ssh morvoso@192.168.134.60` (key auth; used ControlMaster socket
`~/.ssh/sockets/`). NOTE: 4 failed username probes tripped rate-limiting once
(connection reset); back off and reuse the control socket. Hostname `llm-host`.
Sudo granted by user for this project; **not used this session**.

## Box

- Arch Linux, kernel 7.1.8-arch1-3 (same distro as workstation → native
  binaries port directly; no musl needed).
- i9-12900KF (24 threads), 62 GiB RAM (44 available), NVMe 931 GB (692 free).
- No CUDA toolkit on host. Docker rootless-group works. Builder image present:
  `nvidia/cuda:13.0.0-devel-ubuntu24.04`. Build containers need
  `--runtime nvidia` for cuMem* link symbols (prior lore, reconfirmed later).
- GPUs: 3090 #0 23722/24576 MiB used, 44 °C, cap 260 W;
  3090 #1 22648/24576 MiB, 36 °C, cap shows 390 W (doc said 350 default —
  card's max limit differs; we never touch either).

## Running services (do not disturb)

- `llama-qwen` = ghcr.io/ggml-org/llama.cpp:server-cuda-**b10423**, port
  8020→8080, healthy. Prod flags captured in
  `notes/reference/prod/docker-compose.llamacpp.yml`. Notables beyond
  ENGINE.md: `LLAMA_ARG_THINK_BUDGET=4096` + budget message,
  `CUDA_DEVICE_ORDER=PCI_BUS_ID`, `NCCL_P2P_DISABLE=1`, `--fit off`,
  `--no-warmup`, `-ngld 999`, `--image-min-tokens 1024`, `--jinja`.
- searxng :8888, questboard :80/:443, cloudflared.

## Model files (`~/llm/models/qwen38/`)

| file | size | arch | notes |
|---|---|---|---|
| Qwen3.8-27B-UD-Q8_K_XL.gguf | 29.30 GiB | qwen35 | prod; Q8_0 453t/24.5GiB + BF16 53t/4.8GiB + F32 360t; 866 tensors |
| Qwen3.8-27B-UD-Q6_K_XL-v3.gguf | 23.56 GiB | qwen35 | validated fallback |
| Qwen3.8-27B-DFlash2-Q8_0.gguf | 1.92 GiB | dflash | 5 blocks, 5120 embd, 81 tensors |
| Qwen3.8-27B-DFlash2-Q4_K_M.gguf | 1.06 GiB | dflash | |
| mmproj-BF16.gguf | 0.87 GiB | clip | qwen3vl_merger, 27-block ViT, 1152 embd |

## qwen35 metadata (from prod GGUF, via `tandem inspect`)

- 65 blocks (64 + 1 nextn/MTP), n_embd 5120, n_ff 17408, ctx 262144.
- attn: 24 heads / 4 KV, head k=v=256, rms_eps 1e-6, rope base 1e7,
  rope dims 64, sections [11,11,10,0] (IMROPE), full_attention_interval 4
  → attn layers = 3,7,11,…,63 (i.e. `(i+1)%4==0`), GDN elsewhere.
- ssm: conv_kernel 4, groups (H_k) 16, inner 6144, state 128, dt_rank (H_v) 48
  → per-layer conv channels 10240, GDN state 128×128×48 f32.
- vocab 248320 BPE (`pre=qwen35`), bos 248044, eos 248046, pad 248055;
  merges 247587; template 9993 B sha256 12827f24… (== prod live template).
- `general.sampling.*` embedded in GGUF: temp 1.0, top_k 20, top_p 0.95.

## llama.cpp source facts (checkout `~/llm/llama.cpp-dflash2`, PR #27342 @ 5ecbe1a)

- `src/models/qwen35.cpp` — trunk + MTP graph builder (snapshot in
  notes/reference/). MTP = LLM_GRAPH_TYPE_DECODER_MTP, hidden handoff via
  `t_h_nextn`, per-layer `t_layer_inp[il]` outputs exist (DFlash2 PR reads
  trunk hiddens).
- `src/models/delta-net-base.cpp` — three GDN paths: fused
  (`ggml_gated_delta_net`, default on: `cparams.fused_gdn_ar/ch = true`, with
  runtime probe fallback), composite autoregressive, composite chunked (CS 64).
  Rollback: `cparams.n_rs_seq` → K = n_rs_seq+1 snapshots written per step for
  both GDN state and conv state ([TAG_RECURRENT_ROLLBACK_SPLITS]).
- CUDA kernel: `ggml/src/ggml-cuda/gated_delta_net.cu` (14 KB).
- Vendor pin for tandem: tag **b10423** (prod parity).

## Today's runs on the box (all CPU-only, prod untouched)

- `/tmp/tandem inspect` over all four GGUFs — header-only reads.
- Template dumped to /tmp/qwen38-template.jinja, hash-verified vs local copy.
- File copies (scp) of reference sources.
